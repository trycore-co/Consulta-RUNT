<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>{{ titulo }}</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f6f9fc; padding: 20px; color: #333; }
    .container { background: #fff; padding: 20px 30px; border-radius: 10px; box-shadow: 0 0 8px #ccc; }
    h2 { color: #0b5394; }
    .footer { margin-top: 30px; font-size: 12px; color: #777; text-align: center; }
  </style>
</head>
<body>
  <div class="container">
    <h2>{{ titulo }}</h2>
    <p><strong>Estado:</strong> {{ estado }}</p>
    {% if total %}<p><strong>Total de registros:</strong> {{ total }}</p>{% endif %}
    {% if exitosos is defined %}
      <p><strong>Registros exitosos:</strong> {{ exitosos }}</p>
      <p><strong>Registros con error:</strong> {{ errores }}</p>
    {% endif %}
    {% if pdf_path %}
      <p>📎 Se adjunta el reporte generado: {{ pdf_path }}</p>
    {% endif %}
    {% if pdfs_adjuntos %}
      <p>📎 Se adjuntan los reportes generados: {{ pdfs_adjuntos }}</p>
    {% endif %}
    {% if pdf_storage_path %}
      <p>📎 La ruta de almacenamiento local donde se encuentran estos archivos generados es: {{ pdf_storage_path }}</p>
    {% endif %}
    
  </div>

  <div class="footer">
    <p>© {{ now().year }} Sistema RPA RUNT | Correo automático, no responder.</p>
  </div>
</body>
</html>
